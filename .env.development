# Development configuration for request body logging middleware

# Enable development mode features (timing, additional headers, debug logging)
DEVELOPMENT_MODE=true

# Log ALL POST requests, not just configured endpoints (useful for API exploration)
LOG_ALL_POST_REQUESTS=false

# Example: Enable to log all POST requests during development
# LOG_ALL_POST_REQUESTS=true
